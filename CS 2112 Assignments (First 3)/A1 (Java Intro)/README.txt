Wallace Wei
wzw4

On Q10, if you input a number below 0 to skip or an invalid input like a string, it asks you to input again, but if you input a number above file length, it will output blanks, because there's nothing to output

For Q6, when the precondition is violated, that the exponent must not be larger or equal to the length of coefficients array or be less than 0, then my program won't return an error or a warning message, but will just return 0, since the coefficient for something that doesn't exist is 0. It also gives a comment version if one wants to assign "blame" on the user for a bad input. 